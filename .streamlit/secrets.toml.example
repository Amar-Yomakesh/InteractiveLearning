# Streamlit secrets template
#
# LOCAL DEV
#   Copy this file to .streamlit/secrets.toml and fill in your values.
#   secrets.toml is gitignored — never commit it.
#
# STREAMLIT COMMUNITY CLOUD
#   Paste the key=value pairs directly into:
#   App dashboard → ⋮ menu → Settings → Secrets

# Required
OPENAI_API_KEY = "sk-proj-your-key-here"

# Optional — override defaults from config.py
# SCRAPE_BASE_URL    = "https://docs.mulesoft.com/cloudhub/"
# SCRAPE_MAX_PAGES   = "100"
# COLLECTION_NAME    = "cloudhub_docs"
